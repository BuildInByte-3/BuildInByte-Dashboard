import { NextRequest, NextResponse } from "next/server";import { requireApiAdmin } from "@/lib/auth/api";import { inquiryUpdateSchema } from "@/lib/validation";import { assertSameOrigin,noStoreHeaders } from "@/lib/security/request";import { createAdminClient } from "@/lib/supabase/admin";import { audit } from "@/lib/security/audit";
export async function PATCH(request:NextRequest,{params}:{params:Promise<{id:string}>}){const auth=await requireApiAdmin("inquiries:write");if(auth.response)return auth.response;try{assertSameOrigin(request);}catch{return NextResponse.json({error:"Invalid request origin"},{status:403});}const parsed=inquiryUpdateSchema.safeParse(await request.json().catch(()=>null));if(!parsed.success)return NextResponse.json({error:"Invalid inquiry update"},{status:422});const{id}=await params;const{expectedUpdatedAt,status,priority,assignedAdminId}=parsed.data;const changes={...(status?{status}:{}),...(priority?{priority}:{}),...(assignedAdminId!==undefined?{assigned_admin_id:assignedAdminId}:{}),updated_at:new Date().toISOString()};const client=createAdminClient();const{data,error}=await client.from("inquiries").update(changes).eq("id",id).eq("updated_at",expectedUpdatedAt).select("*").maybeSingle();if(error)return NextResponse.json({error:error.message},{status:400});if(!data)return NextResponse.json({error:"Inquiry changed since it was loaded"},{status:409});await audit(auth.admin,"inquiry.update","inquiry",id,changes);return NextResponse.json({data},{headers:noStoreHeaders()});}
